{
    "data": [
      {{ range $index, $e := .Pages }}
      {{ if $index }}, {{ end }}
      {
        "title": "{{ .Title }}",
        "permalink": "{{ .Permalink }}",
        "summary": "{{ .Summary }}"
      }
      {{ end }}
    ]
  }